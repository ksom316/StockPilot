# Feature modules

Future business capabilities belong in isolated folders here. Inventory will remain the only mandatory module; optional modules must not become dependencies of core inventory workflows.
